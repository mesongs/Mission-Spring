<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace로 구분한다 namespace가 다르면 id가 같아도 상관없음 -->
<!-- alias는 sqlMapConfig에, sqlMapConfig는 typeAlias로 거의 채워짐 -->

<mapper namespace="board.BoardDAO">

	<resultMap type="boardVO" id="boardMap">
		<result column="view_cnt" property="viewCnt" />
		<result column="reg_date" property="regDate" />
	</resultMap>
	
	<select id="selectAll" resultMap="boardMap">
		select no, title, writer, to_char(reg_date, 'yyyy-mm-dd') as reg_date
		from t_board
		order by no desc
	</select>
	
	<!-- 게시글 상세 조회, no를 가지고 조회함 -->
	<!-- 파라미터 타입이 int이기 때문에, #{no} 이게 어떤 이름을 와도 상관없음 -->
	<!-- boardMap에서  boardVO(alias에 의한)라고 type을 명시해놨음 조회한 컬럼값이 해당하는 VO에 멤버변수로 들어감 -->
	<select id="selectByNo" parameterType="int" resultMap="boardMap">
			select no, title, writer, content, view_cnt, to_char(reg_date, 'yyyy-mm-dd') as reg_date
			from t_board
			where no = #{no}
	</select>
	
	
	<delete id="deleteByNo" parameterType="int">
			delete
			from t_board
			where no = #{no}	
	</delete>
	
	<insert id="writeBoard" parameterType="boardVO">
		insert into t_board(no, title, writer, content)
		values(seq_t_board_no.nextval, #{title}, #{writer}, #{content})
	</insert>

</mapper>